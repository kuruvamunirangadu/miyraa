# Minimal runtime & dev dependencies
transformers>=4.30.0
tokenizers
datasets
onnx
onnxruntime
onnxruntime-tools
pytest
numpy

# Note: PyTorch is installed explicitly in CI using the CPU wheel index.
# Locally install torch via:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# Pinned runtime/dev requirements. Update pins as needed.
pytest==7.4.0
flake8==6.1.0
fastapi==0.100.0
uvicorn==0.22.0
httpx==0.24.1
starlette==0.29.0
datasets==2.14.2
# Optional CPU-only PyTorch wheel. This requires using the PyTorch CPU index at install time:
# pip install --index-url https://download.pytorch.org/whl/cpu -r requirements.txt
torch==2.5.1+cpu
transformers
onnx
onnxruntime
requests
numpy
